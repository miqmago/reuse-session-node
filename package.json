{
  "name": "reuse-session-case",
  "version": "1.0.0",
  "description": "https://github.com/nodejs/node/issues/27167",
  "main": "src/index.js",
  "scripts": {
    "certs": "[ ! -d ./src/certs ] && ./createCerts.sh ./src localhost pAs$~w0rD || true",
    "start": "npm run certs && node src/index.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^11.13.5"
  }
}
